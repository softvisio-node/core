const { constants } = require( "../util/browser/constants" );

const CONST = {
    "OBJECT_IS_MIXIN": Symbol(),

    "ROOT_USER_NAME": "root",
    "ROOT_USER_ID": "1",
};

module.exports = constants( CONST );
