title: Notifications
permissions: ["*"]

methods:
  read:
    title: Read notifications
    meta:
      type: read
      readMaxLimit: 100
      readDefaultOrderBy: [["date", "desc"]]
    params:
      - name: options
        schema:
          apiRead:
            id: { type: string, conditions: ["="], sortable: true }
            search: { type: string, conditions: ["like"] }
            name: { sortable: true }
            created: { sortable: true }
            enabled: { sortable: true }
