- $id: config
  type: object
  properties:
    enabled: { type: boolean }
    id: { type: ["null", string] }
    url: { type: string, format: url }

  additionalProperties: false
  required: [enabled, id, url]
