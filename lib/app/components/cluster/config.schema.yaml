- $id: config
  type: object
  properties:
    enabled: { type: boolean }
    id: { type: ["null", string] }
    apiUrl: { type: string, format: url }

  additionalProperties: false
  required: [enabled, id, apiUrl]
