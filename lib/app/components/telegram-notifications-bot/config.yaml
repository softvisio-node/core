dependencies:
  - telegram

optionalDependencies: ~

config:
  locales: [en-GB, en-US, ru-RU, uk-UA]

  acl:
    roles:
      read-only:
        name: !l10nt Read-only
        description: !l10nt Bot read-only permissions
        permissions:
          - telegram/bot:read
