server {
    server_name   "";

<% if ( !listenIpFamily || listenIpFamily === 4 ) { -%>
    listen        *:<%- httpPort %> default_server;
<% } -%>
<% if ( !listenIpFamily || listenIpFamily === 6 ) { -%>
    listen        [::]:<%- httpPort %> default_server;
<% } -%>

    return        444;
}
