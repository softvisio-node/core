upstream "<%- proxy.id %>" {
    zone                    "<%- proxy.id %>-stream"
                            1M;
    least_conn;

<% if ( proxy.hasUpstreams ) { -%>
<% for ( const server of proxy.upstreams ) { -%>
    server                  "<%- server %>";
<% } -%>
<% } else { -%>
    server                  "unix:/dev/null" down;
<% } -%>
}
