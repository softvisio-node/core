title: Nginx

methods:
  add-upstream:
    title: Add nginx upstream
    persistentConnectionRequired: true
    params:
      - name: options
        required: true
        schema:
          type: object
          properties:
            server_name: { type: array, items: { type: string }, minItems: 1, uniqueItemd: true }
            client_max_body_size: { type: string }
            cache_enabled: { type: boolan }
            cache_max_size: { type: string }
            cache_inactive: { type: string }
          additionalProperties: false
          required: []
