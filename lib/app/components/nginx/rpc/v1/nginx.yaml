title: Nginx

methods:
  add-upstream:
    title: Add nginx upstream
    persistentConnectionRequired: true
    params:
      - name: options
        required: true
        schema:
          type: object
          properties:
            serverName: { type: array, items: { type: string }, minItems: 1, uniqueItemd: true }
            clientMaxBodySize: { type: string }
            cacheEnabled: { type: boolan }
            cacheMaxSize: { type: string }
            cacheInactive: { type: string }
          additionalProperties: false
          required: [serverName]
