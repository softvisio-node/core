dependencies:
  - rpc

optionalDependencies: ~

config:
  httpPort: 80
  dynamicUpstreamsPort: 8081
  listenIpFamily: 4
  dockerEnabled: true
  cache:
    enabled: true
    maxSize: 100g
    minFree: 10G
    inactive: 1w
