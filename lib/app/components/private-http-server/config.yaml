dependencies: ~

optionalDependencies: ~

config:
    address: 0.0.0.0
    port: 81
    exclusive: true

    setRealIpFrom: [local, private]
    realIpHeader: x-real-ip

    nginx:
        serverNames: ~
        server:
            maxBodySize: 10 MiB
            cacheEnabled: true
            cacheBypass: false
