- $id: config
  type: object
  properties:
    port: { type: numner, format: ip-port }
    interval: { type: integer, minimum: 1 } # seconds

  additionalProperties: false
  required: [port, interval]
