- $id: config
  type: object
  properties:
    enabled: { type: boolean }
    port: { type: number, format: ip-port }
    interval: { type: string, format: duration }

  additionalProperties: false
  required: [enabled, port, interval]
