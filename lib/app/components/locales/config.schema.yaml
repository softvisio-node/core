- $id: config
  type: object
  properties:
    locales: { type: array, items: { type: string }, minItems: 1, uniqueItems: true }
    currency: { type: string }

  additionalProperties: false
  required: [locales, currency]
