module: crypto
type: postgresql
patch: ~

emits: ~

locks:
    - crypto/init
# cron:
#   cleanup:
#     cron: "0 1 * * *" # 01:00 everyday
#     timezone: ~
#     query: |-
#       DELETE FROM api_session WHERE expires <= CURRENT_TIMESTAMP;
