title: Administration

methods:
  revoke-key:
    title: Revoke current encryption key

  revoke-master-key:
    title: Revoke current master key
    params:
      - name: master_key
        schema: { type: string }
