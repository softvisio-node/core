title: Access control lists

methods:
  get-acl-permissions:
    title: Get ACL permissions
    permission: acl:read
    params:
      - name: acl_id
        required: true
        schema: { type: string, aclObjectType: acl }
      - name: acl_object_type
        required: true
        schema: { type: string }

  get-users:
    title: Get ACL users
    permission: acl:read
    params:
      - name: acl_id
        required: true
        schema: { type: string, aclObjectType: acl }

  get-roles:
    title: Get ACL roles
    permission: acl:read
    params:
      - name: acl_id
        required: true
        schema: { type: string, aclObjectType: acl }

  add-user:
    title: Add ACL user
    permission: acl:update
    params:
      - name: acl_id
        required: true
        schema: { type: string, aclObjectType: acl }
      - name: user_id
        required: true
        schema: { type: string }
      - name: options
        schema:
          type: object
          properties:
            enabled: { type: boolean }
            roles: { type: array, items: { type: string }, uniqueItems: true }
          additionalProperties: false

  delete-user:
    title: Delete ACL user
    permission: acl:update
    params:
      - name: acl_id
        required: true
        schema: { type: string, aclObjectType: acl }
      - name: user_id
        required: true
        schema: { type: string }

  set-user-enabled:
    title: Set ACL user enabled
    permission: acl:update
    params:
      - name: acl_id
        required: true
        schema: { type: string, aclObjectType: acl }
      - name: user_id
        required: true
        schema: { type: string }
      - name: enabled
        required: true
        schema: { type: boolean }

  set-role-enabled:
    title: Set ACL user role enabled status
    permission: acl:update
    params:
      - name: acl_id
        required: true
        schema: { type: string, aclObjectType: acl }
      - name: user_id
        required: true
        schema: { type: string }
      - name: role
        required: true
        schema: { type: string }
      - name: enabled
        required: true
        schema: { type: boolean }

  suggest-users:
    title: Suggest users
    permission: acl:read
    params:
      - name: options
        schema:
          read:
            fields:
              acl_id: { type: string, operator: "=", required: true, aclObjectType: acl }
              email: { type: string, operator: "like" }
            order_by: [["email", "desc"]]
            offset: false
            limit: { maxLimit: 20 }
