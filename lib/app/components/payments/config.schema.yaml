- $id: public-config
  type: object
  properties:
    types: false

- $id: config
  type: object
  properties:
    currencies: { type: array, items: { type: string }, minItems: 1, uniqueItems: true }

  additionalProperties: false
  required: [currencies]
