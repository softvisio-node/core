dependencies: ~

optionalDependencies:
  - nginx

config:
  address: 0.0.0.0
  port: 80
  exclusive: true

  nginx:
    apiUrl: ws://nginx/api
    serverName: ~
    clientMaxBodySize: 10m
    cacheEnabled: true
    cacheMaxSize: 10g
    cacheInactive: 1w
