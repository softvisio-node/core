type: object
properties:
  maxPayloadLength: { type: integer, default: 65535 } # 64K
  idleTimeout: { type: integer, default: 0 }
  sendPingsAutomatically: { type: boolean, default: true }
  compress: { type: boolean, default: false } # do not compress rpc messages

required: []
additionalProperties: false
