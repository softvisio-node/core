dependencies:
  - users
  - notifications
  - actionTokens
  - storage

optionalDependencies:
  - cluster

config:
  usersCacheMaxSize: 10_000
  runUpdater: true
  runProcessor: true
  storageFilesMaxAge: 604800 # seconds, 7 data
