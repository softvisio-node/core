title: User profile
permissions: [user]

methods:
  read:
    title: Read user profile

  set-email:
    title: Set user email
    params:
      - name: email
        required: false
        schema:
          type: string

  set-telegram-username:
    title: Set user telegram name
    params:
      - name: username
        required: false
        schema:
          type: string
