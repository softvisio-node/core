// const SEGMENTER = new Intl.Segmenter( "en", { "granularity": "grapheme" } );

// "trim": column.trim, "wordWrap": column.wordWrap
export default function wrap ( string, maxLengthc, options = {} ) {
    return string;
}
