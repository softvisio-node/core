type: object
properties:
  env:
    anyOf:
      - type: "null"
      - type: object
        additionalProperties: { type: string }

  dependencies:
    anyOf:
      - type: "null"
      - type: array
        items: { type: string, format: strict-camel-case }
        uniqueItems: true

  config:
    type: ["null", object]

additionalProperties: false
