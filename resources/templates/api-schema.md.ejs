# <%- options.type.toUpperCase() %> v<%- version %>

### API connect URLs

-   API HTTP URL: `<%- httpURL %>`
-   API WebSockets URL: `<%- wsURL %>`

<!-- tabs:start -->

#### **WebSockets**

<!-- prettier-ignore -->
```javascript
const api = API.new( "<%- wsURL %>" )
```

#### **HTTP**

<!-- prettier-ignore -->
```javascript
const api = API.new( "<%- httpURL %>" )
```

<!-- tabs:end -->
<% for ( const module of modules ) { -%>

## <%- module.title %>
<% if ( module.description ) { -%>

<%- module.description %>
<% } -%>
<% for ( let method of Object.values( module.methods ).filter( method => !method.private ) ) { -%>

<% if ( method.deprecated ) { -%>
### ~~<%- method.title %>~~ `[deprecated]`
<% } else { -%>
### <%- method.title %>
<% } -%>

<!-- tabs:start -->

#### **JavaScript**

<!-- prettier-ignore -->
```javascript
const res = await api.call( "<%- method.id %>"<%- method.markdownSignature ? ", " + method.markdownSignature : "" %> );
```

#### **Shell**

<!-- prettier-ignore -->
```shell
<% if ( method.markdownSignature ) { -%>
curl \
    -X POST \
    -H "Authorization: Bearer <YOUR-API-TOKEN>" \
    -H "Content-Type: application/json" \
    -d '[<%- method.markdownSignature %>]' \
    "<%- httpURL %><%- method.id.substr( 1 ) %>"
<% } else { -%>
curl \
    -H "Authorization: Bearer <YOUR-API-TOKEN>" \
    "<%- httpURL %><%- method.id.substr( 1 ) %>"
<% } -%>
```

<!-- tabs:end -->
<% if ( method.spec.params ) { -%>
<% for ( let param of method.spec.params ) { -%>

-   `<%- param.name %><%- param.required ? "" : "?" %>`<%- getParamDescription( method.id, param ) %>
<% } -%>
<% if ( options.type === "api" ) { -%>
-   **Method access permissions:** <%- method.permissions.map( item => `\`${item}\``).join(", ") %>.
<% } -%>
<% } -%>
<% if ( method.description ) { -%>

<%- method.description %>
<% } -%>
<% } -%>
<% } -%>
